# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files

  - repo: https://github.com/psf/black
    rev: 24.4.2
    hooks:
      - id: black
        args: [--line-length=88]

  - repo: https://github.com/pycqa/isort
    rev: 5.13.2
    hooks:
      - id: isort
        name: isort (python)
        args: ["--profile", "black"]

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.1.6
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.7.0
    hooks:
      - id: mypy
        args: [--ignore-missing-imports, --no-error-summary]
        additional_dependencies: [pydantic, types-requests, types-PyYAML]

  # Pyrefly: Fast type checker by Meta/Facebook (local hook)
  # NOTE: Disabled for initial commit (78 errors remaining, down from 123)
  # TODO: Fix remaining errors in follow-up PRs
  # - repo: local
  #   hooks:
  #     - id: pyrefly
  #       name: pyrefly type check
  #       entry: pyrefly check
  #       language: system
  #       types: [python]
  #       pass_filenames: false
  #       args: ["src"]

  - repo: local
    hooks:
      - id: validate-docs
        name: Validate Documentation Governance
        entry: python scripts/validation/validate-docs.py
        language: system
        types: [markdown]
        pass_filenames: true
